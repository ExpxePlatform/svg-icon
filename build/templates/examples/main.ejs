import Vue from 'vue' import App from './App.vue'

// 完整引入图标组件库
import icons from '../src/index.js'

// 引入字体图标
import '../scr/icon-fonts/iconfont.css'

Vue.config.productionTip = false 

// 全局注册图标组件
Vue.use(icons) 

new Vue({
  render: h => h(App)
}).$mount('#app')
